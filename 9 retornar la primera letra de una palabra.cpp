#include<atdio.h>
#include<atring.h>
#include<atdllb.h>

char L (char * cadena)
{
return cadena (o);
{
int main()
}}
char palabra (10);

printf("Escribe una palabra:");

system ("pause")

return 0;

}